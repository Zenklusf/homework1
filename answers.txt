What is the difference between a merge and a rebase?

A merge keeps the structure of a branch coming out and going back into the master branch, while a rebase changes branch and creates new commits that are on the end of the master branch, creating a linear structure.